apiVersion: 1
policies:
  - orgId: 1
    receiver: discord-anomaly
    group_by:
      - datastream
    group_wait: 10s
    group_interval: 1m
    repeat_interval: 30m
    matchers:
      - severity = critical
